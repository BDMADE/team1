You must have to install pear before installing mongodb driver.then
//install the mongodb driver for php
1.sudo pecl install mongo

//go to php.ini file to add mongo driver

2.sudo nano /etc/php5/apache2/php.ini

//adding 'extension=mongo.so' php.ini file and save it. 

3.extension=mongo.so

restart the apache server

4.sudo /etc/init.d/apache2 restart
